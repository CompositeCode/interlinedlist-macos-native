// OnboardingPlaceholderView
//
// M0 placeholder. The real onboarding window (login/register/reset/OAuth)
// is wired up in the M0 follow-up tasks per PLAN.md §4 / §6.

import SwiftUI

struct OnboardingPlaceholderView: View {
    var body: some View {
        Text("Onboarding")
            .font(.title)
            .foregroundStyle(.secondary)
    }
}
