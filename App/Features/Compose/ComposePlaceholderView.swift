// ComposePlaceholderView
//
// M0 placeholder. Replaced in M2 with the real composer per PLAN.md §6.

import SwiftUI

struct ComposePlaceholderView: View {
    var body: some View {
        Text("Compose")
            .font(.title)
            .foregroundStyle(.secondary)
    }
}
