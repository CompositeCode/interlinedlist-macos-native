// NotificationsPlaceholderView
//
// M0 placeholder. Replaced in M5 with notifications tray per PLAN.md §6.

import SwiftUI

struct NotificationsPlaceholderView: View {
    var body: some View {
        Text("Notifications")
            .font(.title)
            .foregroundStyle(.secondary)
    }
}
