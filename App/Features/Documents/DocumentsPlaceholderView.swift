// DocumentsPlaceholderView
//
// M0 placeholder. Replaced in M4 with the documents feature per PLAN.md §6.

import SwiftUI

struct DocumentsPlaceholderView: View {
    var body: some View {
        Text("Documents")
            .font(.title)
            .foregroundStyle(.secondary)
    }
}
