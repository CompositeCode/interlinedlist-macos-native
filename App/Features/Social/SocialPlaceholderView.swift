// SocialPlaceholderView
//
// M0 placeholder. Replaced in M5 with profiles/follow/requests per PLAN.md §6.

import SwiftUI

struct SocialPlaceholderView: View {
    var body: some View {
        Text("Social")
            .font(.title)
            .foregroundStyle(.secondary)
    }
}
